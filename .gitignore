# OS Files
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# IDE / Editor Files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Docker files we might not want to track (if any, but docker-compose.yml and Dockerfile are explicitly needed, so we don't ignore them)
# Node modules if any
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
